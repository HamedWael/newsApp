import 'package:flutter/material.dart';

class PopularNewsScreen extends StatelessWidget {
  const PopularNewsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Popular'),
    );
  }
}
